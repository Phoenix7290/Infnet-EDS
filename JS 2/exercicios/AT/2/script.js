const numeros = [2, 2, 4, 6, 1, 3, 7, 1, 8, 6];

const primeiroDois = numeros.indexOf(2);
const ultimoDois = numeros.lastIndexOf(2);

console.log(`A posição do primeiro 2: ${primeiroDois}`);
console.log(`O posição do último 2: ${ultimoDois}`);